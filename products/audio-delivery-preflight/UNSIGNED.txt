DEVELOPER-ID-UNSIGNED DEVELOPMENT CANDIDATE

The application and CLI carry only ad-hoc signatures so macOS can execute the
binaries. An ad-hoc signature provides no verified publisher identity. This
archive has not been signed with an Apple Developer ID certificate and has not
been notarized by Apple. macOS Gatekeeper may block or warn about the
application. Do not describe this archive as a normal one-click installation.

The archive is provided only for controlled release validation. Before a paid
public launch, either sign and notarize the app or disclose this limitation
prominently on the product page before purchase.
